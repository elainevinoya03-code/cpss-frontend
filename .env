# Dev API traffic goes through the Vite dev proxy (/api -> http://127.0.0.1:8000),
# so no VITE_API_URL is needed locally. Set VITE_API_URL for production builds
# (e.g. the deployed FastAPI backend URL on Vercel).
